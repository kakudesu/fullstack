function print(value) {
    document.write(value);
}

function clc(num, tang) {
    return num * tang;
}

function hello(deagree) {
    return Math.log(deagree);
}

print(hello(4));
print(clc(3, 4.00));